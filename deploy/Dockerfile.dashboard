# Stage 1: Build Frontend SPA
FROM node:22-alpine AS web-builder
WORKDIR /app/web
COPY web/package*.json ./
RUN npm install
COPY web/ ./
RUN npm run build

# Stage 2: Build Go Dashboard Server using native Go cross-compilation
FROM --platform=$BUILDPLATFORM golang:alpine AS go-builder
ARG TARGETARCH
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=$TARGETARCH go build -ldflags="-s -w" -o /app/dashboard ./cmd/dashboard
RUN CGO_ENABLED=0 GOOS=linux GOARCH=$TARGETARCH go build -ldflags="-s -w" -o /usr/local/bin/reset_password ./cmd/reset_password

# Stage 3: Final Production Image
FROM alpine:3.20
RUN apk --no-cache add ca-certificates tzdata

RUN adduser -D -u 1000 appuser

WORKDIR /app

COPY --from=go-builder /app/dashboard /app/dashboard
COPY --from=go-builder /usr/local/bin/reset_password /usr/local/bin/reset_password
COPY --from=go-builder /usr/local/bin/reset_password /app/reset_password
COPY --from=web-builder /app/web/dist /app/web/dist

RUN mkdir -p /data/epg /app/backups && \
    chown -R appuser:appuser /app /data/epg

USER appuser

ENV DASHBOARD_PORT=8080
ENV PORT=8080
ENV STATIC_DIR=/app/web/dist

EXPOSE 8080
CMD ["/app/dashboard"]
